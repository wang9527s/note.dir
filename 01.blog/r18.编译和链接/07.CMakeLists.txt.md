

```bash
mac的编译产物，同事支持x86和arm架构
set(CMAKE_OSX_ARCHITECTURES "x86_64;arm64")
```


一次 mac 运行dump，添加如下编译选项后，问题解决。  
gpt说是改变了内存布局

```bash
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fsanitize=address")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsanitize=address")
```