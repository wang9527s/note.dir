设置的桥接模式

```bash
export https_proxy=http://192.168.0.198:10808
export http_proxy=http://192.168.0.198:10808
echo 'Acquire::http::Proxy "http://192.168.0.198:10808";' | sudo tee /etc/apt/apt.conf.d/proxy.conf
Acquire::http::Proxy "http://192.168.0.198:10808";
echo 'Acquire::http::Proxy "http://192.168.0.198:10808";' | sudo tee -a /etc/apt/
apt.conf.d/proxy.conf
```

```bash
sudo apt update && sudo apt install openssh-server
sudo systemctl enable ssh
sudo systemctl start ssh
```
```bash
sudo apt install snapd -y
sudo snap install telegram-desktop
```

突然192.168.0.108（虚拟机）和192.168.0.198（物理机）无法ping通过。
排除防火墙和wsl的干扰
重启虚拟机，还是失败。

```
ip a        中看到没有ip
ip route    五出书
```

最后定位到是桥接模式是自动模式，
编辑 -> 虚拟网络编辑器，更改VMnet0，改为桥接至无线网卡