本周需要在iwatch联调一个功能，库是现有的，但我熟悉swift代码。遂使用ai帮我写代码

### 2026.03.23

day01
上午更新iphone，绑定iwatch。  
下午一遍尝试xcode连接iwatch，一边写demo，让**micode + gpt 5.2**帮我写，主要是cpp调用swift接口，以及swift回调的返回。写了一下午，demo勉强完成。

 
### 2026.03.24

day02
想了想，demo还是改为纯swift吧。  

这是我的提示词
```bash
1. 帮我新建一个工程，只在watch上运行，添加如下依赖：
   pod 'MIWBTCore',                      :git => 'git@git.n.xiaomi.com:miwearbluetooth/miwbtcore.git',           :branch => 'master'
  pod 'MIWBTModule/PskBind',            :git => 'git@git.n.xiaomi.com:miwearbluetooth/miwbtmodule.git',         :branch => 'master'
```

使用 **micode + glm 4.7**，效果不太行，两个小时左右过去了，第一步还是没完成，无法编译成功呢。

后续改为**cluade code + opus4.6**，效果很不错。提示一两遍，程序就可以在模拟器运行了。
然后执行下面这个提示词，程序也完成的很好
```bash
2. 添加扫描和绑定蓝牙的功能。蓝牙相关操作使用刚才的pod依赖，这是库的方法，我放到knowledge中了
```

一次提问 大概 几十k的token，下午和晚上 最多二十次的提问，demo就好了。（签名等坑还是要踩的）。

### 对比

这一天半的使用，给这几个模型打个分吧

| 组合                     | 评分    |
| ---------------------- | ----- |
| Claude Code + Opus 4.6 | 90    |
| Micode + GPT 5.2       | 60–70 |
| Micode + GLM 4.7       | 50    |

