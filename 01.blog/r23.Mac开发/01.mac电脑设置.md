
### 快捷键更改

mac的快捷键太难用了，尤其我是windwos/mac双持，使用 Karabiner-Elements

```bash
sudo chown -R mac /opt/homebrew && brew install --cask karabiner-elements
```

启动台中打开 karabiner，按照提示，修改需要修改的系统设置

让ai帮我配置为，我想要的快捷键，下面是提示词

```bash
帮我修改Karabiner-Elements配置，并实现如下快捷键
    Ctrl + C / V / S  → 复制粘贴保存
    Alt + Tab → 切换窗口
    Ctrl + Tab → 切换同应用窗口
    保留常见软件（浏览器等）的 Ctrl+Tab 标签切换
    实现shift切换中英文输入法
```