
### 背景

  程序需要适配到apple watch上，但是底层的一个依赖团队只提供swift源码(接口和Android上还不一样)，需要我这边封装一下，并提供和Android一致的oc接口（库名xxMiWear）。然后micont这边在基于我封装的oc库编写上层逻辑

  依赖团队的开发人员听不配合的，只能自己研究

### 情况

**源码集成**

pod 依赖，可以直接安装源码到项目中，很方便。

**pod转framewrok**

[watch_demo](./resource/watch_demo/)

不想扯皮，使用脚本编译依赖的源码，生成framework；然后我再项目中在使用。  
xxMiWear文件夹，pod转framewrok以及xxMiWear的项目配置文件
