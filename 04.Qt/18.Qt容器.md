#### QMap
```cpp
    // 遍历的过程中删除
    QMap<int,int> map;
    QMap<int,int>::iterator itor=map.begin();
    while (itor!=map.end())
    {
        bool isRemove;
        if (isRemove){
            itor = map.erase(itor);
        }
        else {
            ++itor;
        }
    }
```

#### QList

```cpp
    // 对QList的成员求最大值 C11有类似的写法
    QList<float> tmp;
    auto max=max_element(tmp.begin,tmp.end());  //C++的库函数，对QList容器求最值
    qDebug()<<QStringLiteral("最大值：")<<*max;
    
    // 去重           QSet会自动去重，但不会排序
    list = list.toSet().toList();
```

+ 证明QList的存储方式

```cpp
    QList<int> list;
    QVector<int> vector;

    for(int i=0;i<3;++i){
        QDate d=QDate::currentDate();
        list<<1;
        vector<<1;
    }
    qDebug()<<sizeof(QDate);
    qDebug()<<sizeof(list);
    qDebug()<<sizeof(vector);

    for(int i=0;i<3;++i){
        qDebug()<<&list[i];
    }
    for(int i=0;i<3;++i){
        qDebug()<<&vector[i];
    }
```
