  | # | 队列 | 改前 | 改后 | 效果 |
  | --- | --- | --- | --- | --- | 
  | ① | KCP 接收窗口 | 4096 段 (~5.5MB) | 256 段 (~350KB) | 减少协议层缓存 | 
  | ② | buf_queue_ | 不存在 | 新增，31~200 令牌 | 加了背压，防止③无限增长 |
  | ③ | main_handler_ 队列 | 无界 | 被②约束 | 不再无限堆消息 |
  | ④ | BlockingBuf | 8MB | 8MB（未改） | — |  
  | ⑤ | write buf | 8MB | 8MB（未改），加了 flush | 仅加速落盘 |
  
  大概明白了，main_handler_是解密和和控制信令等逻辑。
  加入2，现在123配合起来，有点像修改了滑动窗口的大小，降低网络数据的接收。
  
  还有一个可能需要优化的点是
  然后处理完成后写入BlockingBuf （8MB）中，缓冲区满后，copy一份（8M）到其他线程，然后写入文件。
  现在就是copy的时候，内存不足，dump

  后续修改缓冲区为1MB，防止内存爆掉


