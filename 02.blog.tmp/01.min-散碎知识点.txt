linux qt sleep 睡眠函数头文件是#include <unistd.h>
命令行编译: mkdir build & cd build & cmake ..    然后就可以在build文件夹中，直接make
判断电脑是否是UEFI启动模式： [ -d /sys/firmware/efi ] && echo UEFI || echo notUEFI
判断当系统是否是运行在虚拟机中
    systemd-detect-virt，也可以调用systemd-detect-virt -v -q，通过返回值echo $?来判断
passwd 命令可以用户修改密码
安装kvm虚拟机: sudo apt install -y libvirt0 libvirt-daemon qemu virt-manager \
    bridge-utils libvirt-clients python-libvirt qemu-efi uml-utilities virtinst \
    qemu-kvm qemu-system
鼠标被grub住，解除grub
    setxkbmap -option grab:break_actions
    xdotool key XF86Ungrab
pstree                                          # 查看程序启动顺序
systemd-analyze blame                           # 查看每个服务的启动耗时
新用户的默认配置之/etc/skel: 该目录下的文件，在新建用户的时候都会原封不动的复制到新用户的$HOME目录中
enca 可以查看文件编码
sudo systemctl suspend                          # 待机命令
sudo systemctl hibernate                        # 休眠命令
技能等级
    了解：基本语法了解、基本概念知道，愿意真的做这个
    熟悉：常见的库知道，若有框架，会用其中的一两个，并且做出过东西
    熟练：做面试题可以用来做白板语言，相关框架基本能上手；看看文档可以加入开发；看到错误信息基本可以推测问题出现在哪
    精通：。。。再见！
bcompare=4.4.3-26655设置取消时间戳比较: Rules->取消Compare timestamps的勾选
Windows和Linux都支持NTFS文件格式
systemctl查看系统服务的状态，有些必须加上 --user 
    systemctl status systemd-timesyncd.service
    systemctl --user  status pulseaudio.service
切换bash 和 查看bash
    echo $SHELL
    chsh -s /bin/bash
sudo iptables -t mangle -L                        # iptables    网络
qdbus org.kde.KWin /KWin showDebugConsole        # deepin：窗管的工具软件
Ventoy制作启动盘        sudo sh Ventoy2Disk.sh -i /dev/sdb
屏保确实是用来保护屏幕的                            # 没想到竟然真的是一个正经的保护程序
sudo udevadm monitor                # 监听外设的插拔 udev的日志
markdown教程：https://www.runoob.com/markdown/md-advance.html